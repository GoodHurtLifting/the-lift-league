plugins {
    id "com.android.application"
    id "com.google.gms.google-services"
    id "com.google.firebase.crashlytics"
    id "org.jetbrains.kotlin.android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.lift_league"
    compileSdk = 35
    buildToolsVersion = "34.0.0" // Safe current version
    ndkVersion = "28.0.13004108"

    defaultConfig {
        applicationId = "com.example.lift_league"
        minSdk = 23
        targetSdk = 35
        versionCode = flutter.versionCode
        versionName = flutter.versionName
        manifestPlaceholders = [
                appAuthRedirectScheme: "com.example.lift_league"
        ]
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
        coreLibraryDesugaringEnabled true
    }

    kotlinOptions {
        jvmTarget = "1.8" // ✅ Kotlin requires string, not JavaVersion enum
    }

    signingConfigs {
        release {
            storeFile file("C:/Users/ryanl/keystores/liftleague-release.jks")
            storePassword RELEASE_STORE_PASSWORD
            keyAlias "liftleague"
            keyPassword RELEASE_KEY_PASSWORD
        }
    }

    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            shrinkResources false
        }
    }
}

flutter {
    source = "../.."
}

dependencies {
    implementation 'com.google.firebase:firebase-analytics:22.4.0'
    implementation 'com.google.firebase:firebase-crashlytics:19.4.4'
    coreLibraryDesugaring 'com.android.tools:desugar_jdk_libs:2.1.5'
}
